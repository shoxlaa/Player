using System;

using System.Collections;

namespace ConsoleApp1
{

    enum Buff {Speed, Invisibility, Saturation  }
    enum Debuff {Slownes, Blindness, Hunger }

    class Program
    {
        static void Main(string[] args)
        {

          
            Player Culler= new Player(); 

          int valu = +Culler;
            valu = +Culler;
            valu=-Culler;
           
           

            Console.WriteLine(valu); 

            Console.ReadLine();
             
        }
    }
}